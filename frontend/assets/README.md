# Assets

- `icon-dev.png`: usado automaticamente pelo profile EAS `development`.
- `icon-prod.png`: usado automaticamente pelo profile EAS `preview`.
- `icon.png`: fallback de produção.

A seleção acontece no `app.config.js` via `APP_VARIANT`.
